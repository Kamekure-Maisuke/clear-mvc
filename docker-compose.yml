version: "3.7"

services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
      - ./src:/var/www/html/src
    depends_on:
      - app

  app:
    build: .
    volumes:
      - ./src:/var/www/html/src
